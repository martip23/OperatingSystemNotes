#include <stdio.h>

int main() {
		int numCourses[12];
	char studentName[12][20];
	
	int i = 0;
	while (!feof(stdin)) {
		scanf(" %s %d", studentName[i], &numCourses[i]);
		i++;
	}
	char studentName[12][20];
	
	
	return 0
}